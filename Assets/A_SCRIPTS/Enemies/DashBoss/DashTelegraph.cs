using UnityEngine;

// LineRenderers removidos. Este componente puede eliminarse si ya no lo referenciás en el inspector.
// Se mantiene vacío para no romper referencias existentes en la escena.
public class DashTelegraph : MonoBehaviour
{
    public void Show(Vector3 from, Vector3 to, float telegraphDuration) { }
    public void Hide() { }
}